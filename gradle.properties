# Single source of truth for the project version and group across all modules.
# Bump `version` here before a release (Maven Central rejects re-publishing an existing version).
# See PUBLISHING.md.
group=io.codelaser
version=0.8.2
