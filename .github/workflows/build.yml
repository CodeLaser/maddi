name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# A newer push to the same branch supersedes an in-flight run.
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # The term list for the internal-names guard must never be committed: a list of the names
  # to redact is a decoder ring for the redaction. The scan itself cannot run here without
  # the list (see CONTRIBUTING.md), but this half needs nothing and catches the one mistake
  # that would undo the redaction.
  guard:
    name: internal-names guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - name: The term list must not be in the repository
        run: |
          if git ls-files --error-unmatch .githooks/internal-names.txt >/dev/null 2>&1; then
            echo "::error file=.githooks/internal-names.txt::The internal-names term list is committed."
            echo "It must live outside the repository — see CONTRIBUTING.md, 'Names that must not appear'."
            exit 1
          fi
          test -f .githooks/internal-names.txt.example || {
            echo "::error::.githooks/internal-names.txt.example is missing; contributors have no format reference."
            exit 1
          }
          echo "OK: term list absent, example present."

  # Every commit in a pull request must carry a Signed-off-by trailer: the Developer
  # Certificate of Origin (see DCO and CONTRIBUTING.md). `git commit -s` adds it.
  dco:
    name: DCO sign-off
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Every commit carries Signed-off-by
        env:
          BASE: ${{ github.event.pull_request.base.sha }}
          HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          missing=0
          for c in $(git rev-list --no-merges "$BASE".."$HEAD"); do
            if ! git log -1 --format='%B' "$c" | grep -qiE '^Signed-off-by: .+ <.+@.+>'; then
              echo "::error::$(git log -1 --format='%h %s' "$c") — no Signed-off-by"
              missing=1
            fi
          done
          if [ "$missing" = 1 ]; then
            echo "Add sign-off with:  git commit --amend -s   (or  git rebase --signoff $BASE)"
            echo "What you are certifying is in the DCO file at the repository root."
            exit 1
          fi
          echo "OK: all commits signed off."

  build:
    name: build + fast tests (JDK ${{ matrix.java }})
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        java: ['26']

    steps:
      - uses: actions/checkout@v7

      # No Gradle toolchain is declared, so the build runs on whatever JAVA_HOME provides.
      # Development happens on JDK 26; the openjdk front end reaches into jdk.compiler
      # internals, so running on an older JDK than the one that built it will not work.
      # KEEP Temurin. It ships no `jmods/` directory, which makes this the only environment that
      # actually exercises maddi's runtime-image fallback (JavaInspectorImpl case "jmod" ->
      # addModuleFromRuntimeImage, via the jrt filesystem). TestRuntimeImageFallback skips itself
      # whenever a jmods/ dir exists, so on a typical developer JDK it never runs at all.
      # Confirmed: that test PASSED here on 2026-07-22, its first real execution.
      # Switching to a jmods-shipping distribution (zulu, oracle, ...) would silence that coverage.
      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}

      # Diagnostic, never fails: makes the jmods question answerable from the log alone,
      # whichever way the build goes. Remove once CI is settled.
      - name: Report JDK layout
        run: |
          echo "JAVA_HOME=$JAVA_HOME"
          java -version 2>&1
          if [ -d "$JAVA_HOME/jmods" ]; then
            echo "jmods present: $(ls "$JAVA_HOME/jmods" | wc -l) files"
            ls "$JAVA_HOME/jmods" | head -5
          else
            echo "jmods ABSENT at $JAVA_HOME/jmods"
            ls -la "$JAVA_HOME"
          fi

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      # `build` runs `test`, which excludes @Tag("slow") -- see
      # buildSrc/src/main/kotlin/java-library-conventions.gradle.kts.
      #
      # Do NOT add `slowTest` here. The large-corpus smoke tests need external corpora
      # (test-oss, testarchive) that do not exist on a runner; without them each test
      # skips via a JUnit assumption and the job reports a green that proves nothing.
      # See AGENTS.md "Commands" before changing this.
      - name: Build and run fast tests
        run: ./gradlew build --stacktrace

      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: test-reports-jdk${{ matrix.java }}
          path: |
            **/build/reports/tests/**
            **/build/test-results/**
          retention-days: 7
          if-no-files-found: ignore
