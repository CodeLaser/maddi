[[maven-plugin]]
= The Maven plugin

The Maven plugin (`maddi-mvnplugin`) derives the input configuration from the Maven project model
(compile and test source roots, and the dependency classpath resolved via Maven's own resolver) and,
like the Gradle plugin, hands it to the openjdk `RunAnalyzer`.

== Goals

[cols="1,3",options="header"]
|===
| Goal | Does

| `run` | Parse, prep- and modification-analyze the project; write results.
| `compile-analysis-hints` | Compile hand-written analysis-hint sources into result `.json` files
(<<analysis-hints>>, use case 2).
| `write-analysis-hints` | Generate first-cut analysis-hint skeletons for the library types the
project calls into (use case 3).
| `write-input-configuration` | Write the computed input configuration to JSON.
| `statistics` | Method-call-frequency histograms.
|===

== The `--add-exports` requirement

Unlike Gradle (which forks a worker), a Maven mojo runs in-process, so the Maven JVM itself must be
started with the javac `--add-exports` from <<overview>>. Put them in `.mvn/jvm.config` or `MAVEN_OPTS`:

[source]
----
# .mvn/jvm.config
--add-exports jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED
--add-exports jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED
--add-exports jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED
--add-exports jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED
--add-exports jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED
----

// TODO: publishing coordinates + the <plugin> stanza. The plugin currently builds in-tree (as a Gradle
// module) but its Maven descriptor (META-INF/maven/plugin.xml) is not yet generated, so it is not yet
// installable/runnable as a standard Maven plugin. Document the usage once that is wired.
[NOTE]
====
*Status.* The plugin's sources build in-tree and are aligned with the Gradle plugin, but the Maven
plugin *descriptor* is not yet generated, so it cannot yet be invoked as a normal Maven plugin. This
section will be completed once packaging is in place.
====
